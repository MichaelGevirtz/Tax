export default function ResultsPage() {
  return (
    <main
      style={{
        maxWidth: "var(--max-content-width)",
        margin: "0 auto",
        padding: "var(--space-3xl) var(--mobile-padding)",
        textAlign: "center",
      }}
    >
      <h1>תוצאות</h1>
      <p style={{ color: "var(--color-text-secondary)", marginTop: "var(--space-md)" }}>
        עמוד זה בבנייה — TASK-UI-003
      </p>
    </main>
  );
}
